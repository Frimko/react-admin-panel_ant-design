import React from 'react';
import renderer from 'react-test-renderer';
import App from './App';

it('mount app', () => {
  //const renderedValue = renderer.create(<App/>);
  //renderedValue.unmount();
});
