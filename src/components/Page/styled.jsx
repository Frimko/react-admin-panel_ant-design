import styled from 'react-emotion'
import { Layout } from 'antd';

export const PageBody = styled(Layout.Content)`
    margin: 24px 16px;
     padding: 24px;
      background: #fff; 
      minHeight: 280px
`;
